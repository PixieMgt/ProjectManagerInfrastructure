events {}

http {
  server {
    listen 80;

    # Frontend
    location / {
      proxy_pass http://frontend:3000;
    }

    # Backend
    location /api/ {
      proxy_pass http://backend:3001/api/;
    }
  }
}
